spring:
  datasource:
    username: musuixin
    password: CSRQ19971031yu
    url: jdbc:mysql://rm-bp1783uv4c566u27tmo.mysql.rds.aliyuncs.com/online_learning?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=GMT%2B8
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      idleTimeout: 10000
      minimumIdle: 5
      maximum-pool-size: 10
      max-lifetime: 30000
      connection-test-query: SELECT 1
  application:
    # 服务名
    name: online-learning-LoginRegister-web
  cloud:
    sentinel:
      transport:
        dashboard: 47.104.253.73:8888
  redis:
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        max-wait: -1ms
        min-idle: 0
    host: 39.105.154.17
    port: 6379
    password: 950405
    timeout: 10000
  servlet:
    multipart:
      max-file-size: 20MB
server:
  # 服务端口
  port: 8000
management:
  # 端点检查（健康检查）
  endpoints:
    web:
      exposure:
        include: "*"
logging:
  level:
    top.musuixin: trace
  file: F:\logger\online-learning-LoginRegister-web.log
knife4j:
  production: true
  # 开启Swagger的Basic认证功能,默认是false
  basic:
    enable: true
    ## Basic认证用户名
    username: root
    ## Basic认证密码
    password: 123456